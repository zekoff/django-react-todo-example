ports:
  - port: 8000
    visibility: public
    onOpen: open-preview

tasks:
  - before: |
      pip install -r backend/requirements.txt
      echo $(pwd)
    init: |
      python backend/manage.py migrate
      python backend/manage.py createsuperuser --noinput --username zekoff --email zekoff@gmail.com
    command: |
      echo $(pwd)
      python backend/manage.py runserver